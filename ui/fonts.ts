import { Anton } from "next/font/google";

export const headings = Anton({ subsets: ["latin"], weight: ['400'] });
export const logo = Anton({ subsets: ["latin"], weight: ['400'] });
export const buttons = Anton({ subsets: ["latin"], weight: ['400'] });
